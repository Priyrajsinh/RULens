"""Streamlit dashboard: engine selector -> forecast + band + active circuits.

Streamlit is imported lazily inside run, so importing this module has no side
effects.
"""

from __future__ import annotations


def run() -> None:
    """Entry point for ``streamlit run``; renders the dashboard."""
    raise NotImplementedError
