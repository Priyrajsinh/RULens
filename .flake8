[flake8]
max-line-length = 88
# E203 (whitespace before ':') and W503 (line break before binary operator)
# conflict with black's formatting; ignore them per black's recommendation.
extend-ignore = E203, W503
exclude =
    venv,
    .git,
    __pycache__,
    build,
    dist,
    .dvc
