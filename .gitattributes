# Normalize line endings to LF in the repo so the Linux CI and GPU clones see
# the same bytes the gates were run against on Windows.
* text=auto eol=lf

*.png binary
*.gif binary
*.jpg binary
*.pdf binary
